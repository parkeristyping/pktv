<%= form_for @song do |f| %>
  <%= f.label :source_url, 'Enter a link to youtube or soundcloud or whatever' %>
  <br>
  <%= f.text_field :source_url %>
  <br>
  <%= f.submit 'Karaoke it!' %>
<% end %>
